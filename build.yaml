targets:
  $default:
    builders:
      json_serializable:
        options:
          any_map: true
      freezed:
        options:
          json_serializable: true
        generate_for:
          - lib/src/**/*.dart
